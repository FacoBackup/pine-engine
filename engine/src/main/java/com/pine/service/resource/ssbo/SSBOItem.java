package com.pine.service.resource.ssbo;

import com.pine.service.resource.ubo.UBOItem;

public class SSBOItem extends UBOItem {
    public SSBOItem(int offset, int dataSize, int chunkSize) {
        super(offset, dataSize, chunkSize);
    }
}
